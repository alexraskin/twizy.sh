# twizy.sh

A terminal-style personal website built with Go WASM on Cloudflare Workers.

## Commands

```
npm start      # run dev server
npm run build  # build
npm run deploy # deploy to Cloudflare
```
